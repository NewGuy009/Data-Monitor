# M3 实施计划：历史行情分析与规则引擎

## 1. 文档控制

### 1.1 目标

M3 为单只自选股建立一个可在设备本地运行、可解释的分析流水线：

```text
已校验的历史 Bar
  -> 指标与形态计算
  -> 规则评估
  -> 持久化信号记录
  -> 详情页解释
```

输出是分析信号，不是自动交易指令。Android 通知、后台轮询和日内高频监控属于 M4 范围。

### 1.2 范围

包含：

- 单股票历史行情分析，首版只使用已确认的日线 Bar。
- 可复用的日/周/月数据契约与趋势过滤能力。
- 技术指标、基础 K 线形态、规则、本地信号记录和历史回放。
- 不依赖 Python 服务、可离线运行的 Kotlin 原生计算。

不包含：

- 自动下单或投资建议。
- 将腾讯当日分钟快照作为历史分钟回放数据。
- 完整横截面选股、Alpha Zoo 导入、IC/IR 研究或多股票排序。
- Android 通知投递、前台监控和后台监控。

### 1.3 不可妥协的质量规则

- 领域模型和计算代码必须是纯 Kotlin，不依赖 Compose、Room、Retrofit 或 Android API。
- 重要逻辑和业务代码必须按 `开发需知.md` 添加简洁注释。
- 每个分析结果必须携带 Bar 周期、复权方式、Provider、分析截止点和数据质量状态。
- 结果可以不可用；不得用 0、伪造指标值或伪造信号替代缺失、无效或预热期数据。
- 任何计算、回放和测试都不得读取晚于其声明分析截止点的 Bar。
- 必须兼容已有 M2 图表行为、Provider 选择、缓存隔离和 Room 迁移。

### 1.4 完成定义

只有当一只拥有足够已确认日线历史的自选股能够完成以下事项时，M3 才算完成：

1. 从已校验的历史 Bar 生成可追溯的分析结果。
2. 在不泄漏未来数据的前提下计算基础指标和选定形态。
3. 评估已启用规则，并持久化一条幂等的本地信号记录。
4. 在详情页展示结果、证据和数据截止点。
5. 对同一规则进行确定性的历史回放。

## 2. 生命周期与质量门禁

| 门禁 | 进入条件 | 必需证据 | 退出决策 |
| --- | --- | --- | --- |
| G0 范围基线 | M2 详情和 K 线数据链路可用 | 当前限制清单、M3 范围、排除项 | 批准 M3 范围 |
| G1 需求确认 | G0 已批准 | 用户故事、规则模板、验收标准 | 冻结首版需求 |
| G2 架构确认 | G1 已批准 | 领域模型、依赖图、持久化方案、错误模型 | 实现前批准契约 |
| G3 数据就绪 | G2 已批准 | 历史 Bar 校验器与测试夹具 | 允许实现指标 |
| G4 分析就绪 | G3 已批准 | 指标注册表、基础公式测试、无前视测试 | 允许实现规则 |
| G5 规则就绪 | G4 已批准 | 规则模型、评估证据、幂等测试 | 允许 UI 与持久化集成 |
| G6 候选发布版 | G5 已批准 | 回放报告、Room 测试、UI 测试、回归构建 | 人工验收与 M3 收尾 |

不能因为代码能够编译就进入下一门禁，必须先具备该门禁要求的验证证据。

## 3. 第 0 阶段：现状评估与需求基线

### M3.0.1 现状评估

目的：识别哪些 M2 契约可以复用，哪些缺口会阻塞可靠分析。

评估任务：

- 审查 `Candle`、`CandlePeriod`、`CandleAdjustment`、Provider ID、市场契约、K 线缓存键和 Room 迁移。
- 确认 Provider 身份应属于每根 `Candle` 还是加载序列元数据；除非有明确需要，避免在每根 Bar 重复不可变的序列元数据。
- 确认腾讯日/周/月线响应、当前缓存保留策略、最大获取 Bar 数，以及最新日线在分析时是否已确认。
- 梳理现有校验逻辑，特别是被丢弃的畸形数据行，并识别坏数据仍可能进入分析的路径。
- 记录限制：腾讯 `minute/query` 仅提供当日快照，不是历史分钟数据源。

交付物：在 `Development Notes.md` 中记录可复用决策、缺口和风险。

验证目标：

- 源到分析的数据流图能指出当前每个边界：Provider 解析器、Repository、内存缓存、Room 缓存、ViewModel、图表。
- 每个 M3 必需字段都有明确来源，或明确标记为“不可用”。
- M3 设计不假定存在历史分钟数据，也不假定 M2 未提供的 Provider 能力。

退出标准：批准 G0 范围基线，并记录未解决的数据限制。

### M3.0.2 首版需求

M3 首版支持以下用户结果：

- 用户可查看股票是否有足够且有效的日线历史供分析。
- 用户可检查基础趋势、动量、波动和成交量状态，看到底层数值与截止日期。
- 用户可创建少量确定性的规则模板。
- 规则命中时生成一条可追溯的本地信号记录，而不是笼统的“买入”或“卖出”声明。
- 开发者可在不泄漏未来数据的情况下，对同一规则进行历史 Bar 回放。

初始规则模板：

1. EMA 金叉/死叉。
2. RSI 超卖回升。
3. 放量突破近期区间。
4. 阳包阴/阴包阳确认。

延期规则：

- MACD/RSI 背离，等待 Swing Point 契约与测试夹具稳定后实现。
- 多周期共振，等待单周期规则验证完成后实现。
- 可视化组合规则编辑器，等待模板持久化与评估稳定后实现。

验证目标：

- 每个模板都有明确的输入周期、最小 Bar 数、条件、方向、证据字段和冷却/幂等行为。
- 验收示例能区分“当前趋势状态”和“新发生的交叉事件”。

退出标准：完成 G1 需求确认；除正确性和安全性修复外，新增功能进入后续待办。

## 4. 第 1 阶段：架构与详细设计

### M3.1 历史 Bar 契约与数据质量

设计任务：

- 定义 `HistoricalBarSeries` 或等价的分析输入边界，携带股票身份、Bars、周期、复权、币种、成交量单位、Provider ID、市场时区、获取时间与分析截止点。
- 保持 `Candle` 为归一化 OHLCV 值对象；除非分析专用类型确实消除歧义，序列级元数据应保留在序列级。
- 定义 `HistoricalBarQuality`：`COMPLETE`、`PARTIAL`、`GAPPED`、`INVALID`、`INSUFFICIENT_HISTORY`。
- 为空数据、时间戳顺序、重复 Bar、无效 OHLC、非有限数值、负成交量/成交额、混用复权/币种/单位、最新 Bar 不可用或未完成等情况定义结构化 `HistoricalBarIssue`。
- 定义市场感知的缺口策略。周末、节假日、停牌、上市初期都会导致日历日期缺失，因此首版 `GAPPED` 只能依赖明确的预期交易日历或保守的“异常缺口”状态。
- 定义最新 Bar 确认策略。日线分析只使用已确认的最后一根日线；无法确认时标记为 `PARTIAL`，事件规则不得触发。

目标架构：

```text
Provider 解析器 -> 归一化 Candle 序列 -> HistoricalBarValidator
    -> HistoricalAnalysisInput -> 指标/形态 -> 规则
    -> RuleEvaluation -> 信号 Repository -> Room/UI
```

验证目标：

- `HistoricalBarValidator` 是纯 Kotlin、可单元测试的组件，不依赖 Repository 或 UI。
- 校验器返回结构化问题和质量状态，不得静默删除坏 Bar 后报告成功。
- 有效的 QFQ 与 RAW 序列必须隔离，Provider、周期、币种和数量单位差异也必须隔离。
- 测试覆盖空数据、单 Bar、历史不足、无效 OHLC、非有限价格、负成交量、重复、倒序、混合元数据与保守缺口情形。

退出标准：G3 数据就绪。只有 `COMPLETE` 序列可产生最终事件规则命中。

### M3.2 分析契约与依赖边界

设计任务：

- 定义 `HistoricalAnalysisInput`、`AnalysisSnapshot`、`IndicatorDefinition`、`IndicatorSeries`、`IndicatorValue` 和明确的不可用结果表示。
- 定义 `Indicator` 与 `IndicatorRegistry`；每个指标声明 ID、所需字段、支持周期、最小预热 Bar 数、参数和输出结构。
- `PatternDetector` 与指标分离；检测器返回关联 Bar 时间戳与价格的证据，而不是只有展示字符串。
- 定义 `AnalysisCutoff`，要求每个计算器只处理截止点及之前的 Bar。
- 围绕用例定义 Repository 接口，例如 `AnalyzeStockHistory`、`EvaluateRulesForStock`、`ReplayRuleHistory`；UI 不得直接计算指标。

依赖规则：

```text
ui -> domain use cases/interfaces <- data repositories/Room
domain analytics -> domain historical models
```

验证目标：

- 领域分析代码可在 JVM 上编译和单测，不需要 Android Instrumentation。
- 指标注册表可以拒绝重复 ID 和不支持/缺少的输入。
- 预热期和不可用结果在整个 API 中保持显式表达。
- 截止点测试证明追加未来 Bar 不会改变更早快照。

退出标准：完成 G2 架构确认，并批准 G4 分析就绪前置条件。

### M3.3 持久化、错误与 UI 设计

设计任务：

- 建模 `Rule`、规则条件树、`RuleEvaluation`、`SignalRecord` 以及持久化的规则/信号实体。
- 在实现前确定 Room 表结构和迁移。信号身份为 `ruleId + stock identity + period + adjustment + providerId + signalBarTimestamp + direction`。
- `RuleEvaluation` 保留未命中原因和命中证据；只持久化耐久信号和审计/回放所需数据。
- 定义分析 UI 状态：加载、历史不足、部分/未确认数据、无效数据、分析就绪、无规则命中、规则命中。
- 详情页分析摘要优先采用紧凑只读形式；规则创建先使用模板，M3 不构建通用 JSON 编辑器。

验证目标：

- 每个持久化实体都有所有权边界、主键、迁移影响和保留策略。
- UI Mock 状态可在不依赖网络响应时渲染每一种分析结果。
- 未展示规则证据与数据截止点时，任何状态都不得标记为“买入”或“卖出”。

退出标准：开始 Room 与 Compose 实现前，存储和 UI 契约已评审。

## 5. 第 2 阶段：增量实现

### M3.4 实现数据质量边界

实现任务：

- 在领域层增加历史分析模型和 `HistoricalBarValidator`。
- 改造现有详情 Repository，使分析接收经校验且元数据完整的序列，而不是原始 `List<Candle>`。
- 即使分析拒绝某个序列，也必须保留现有 M2 图表渲染；图表展示和分析资格是独立状态。
- 增加表达有效与无效 Provider 归一化 Bar 序列的测试夹具。

验证目标：

- 单元测试覆盖所有 `HistoricalBarIssue` 分支。
- Repository 测试证明无效序列不会传给分析，也不会作为“分析就绪”缓存写入。
- 既有 K 线解析器、Room 缓存和详情 Repository 测试仍保持通过。

退出标准：执行 `:app:testDebugUnitTest` 和 `git diff --check` 后通过 G3。

### M3.5 实现指标注册表与基础指标

实现任务：

- 实现注册表和通用滚动窗口辅助函数，明确预热与有限数值行为。
- 分别以纯计算器实现 `SMA`、`EMA`、成交量均线、`MACD`、`RSI`、`BOLL`、`ATR`、`OBV`。
- 建立稳定的参数模型。默认值需要版本化并出现在证据中；修改默认值不得静默重解释历史信号。
- 从最后一个已确认 Bar 与指标序列构建 `AnalysisSnapshot`。

验证目标：

- 公式测试使用小型手算夹具和已知参考夹具。
- 每个指标测试预热期、平坦价格、零/缺失成交量、非有限输入拒绝和最后 Bar 对齐。
- 回归测试证明在 T 时刻的计算结果，无论后续 Bar 是否存在都完全一致。

退出标准：基础指标具备确定性且无前视测试通过后，G4 通过。

### M3.6 实现技术状态与形态检测器

实现任务：

- 从 EMA 关系、价格位置和可选强度过滤器推导趋势状态。
- 从 RSI、BOLL、成交量均线和 OBV 推导震荡/成交量状态。
- 在最后两根已确认 Bar 上实现 EMA 交叉事件，并与持续的多头/空头状态分离。
- 实现吞没、实体/影线分类、区间识别与区间突破，并使用明确阈值。
- 仅引入 Swing Point 接口和夹具；等其行为评审完成后再实现 MACD/RSI 背离。

验证目标：

- 交叉事件测试证明单次交叉只发出一次，关系未改变时不得重复触发。
- 每种形态都包含命中、临界未命中和历史不足用例。
- 每个推导状态都返回源数据值、阈值/参数、源 Bar 时间戳和机器可读原因码。

退出标准：指标和形态输出可不经 UI 专用字符串解析而直接被规则消费。

### M3.7 实现规则评估与信号幂等

实现任务：

- 实现指标阈值、EMA 交叉、放量突破和吞没形态的强类型原子条件。
- 实现 `ALL`、`ANY`、`NOT` 条件组，并以支持短路的方式收集证据。
- 实现 `RuleEvaluator`，返回命中、未命中、不可用和被数据质量阻断的结果。
- 实现信号键和去重策略；通知冷却将在后续控制通知资格，当前幂等性保护历史事实。
- 增加首版规则模板及其参数校验。

验证目标：

- 每次规则评估都携带分析截止点和决策证据。
- 同一规则对同一根 Bar 的重复评估产生相同信号键，且不会生成重复信号。
- 修改规则版本、方向、周期、Provider、复权方式或信号 Bar 时，必须形成不同评估上下文。
- 组合规则证明 `ALL`/`ANY`/`NOT` 的逻辑和证据归属正确。

退出标准：G5 规则就绪，所有模板规则都有确定性夹具。

### M3.8 实现 Room Repository 与用例

实现任务：

- 增加规则/信号的 Room 实体、DAO、Repository、Hilt 绑定与迁移。
- 持久化规则配置和解释既往命中所需的不可变信号证据。
- 实现分析股票、评估已启用规则、观察近期信号、创建/更新/禁用模板规则的用例。
- 定义信号保留/清理策略，不能删除可见记录所需的审计证据。

验证目标：

- DAO 测试覆盖插入、更新、启用筛选、身份唯一性、近期信号倒序查询和迁移。
- Repository 测试证明重复评估不能创建重复信号记录。
- 持久化失败时，UI 不得声称信号已保存。

退出标准：耐久规则和信号无需 UI 依赖即可工作，并通过单元测试。

### M3.9 实现详情摘要与模板规则 UI

实现任务：

- 在股票详情增加紧凑分析区：数据质量/截止点、趋势状态、关键数值和近期信号记录。
- 增加明确的不可用与部分数据展示，保持既有图表和报价行为。
- 增加模板选择和参数编辑，持久化前需校验。
- 使用清晰的状态文本和证据，不得仅依赖颜色表达方向或质量。

验证目标：

- Compose 测试覆盖加载、历史不足、部分/无效数据、就绪无信号、就绪已命中信号。
- 模板创建测试证明无效参数不能保存，有效模板会出现在启用规则列表中。
- 无障碍检查覆盖重要内容描述、文字缩放和可读错误状态。

退出标准：用户能够理解分析为何不可用，或某条信号为何被记录。

### M3.10 实现历史回放

实现任务：

- 实现逐 Bar 回放执行器，使每个历史截止点只能看到当时可见的 Bar。
- 复用生产环境的校验器、计算器和评估器，不得另建回放公式。
- 记录回放观察值：信号日期、规则版本、输入元数据、证据和可配置的未来 N Bar 观察收益。
- 回放与完整投资组合回测保持分离：M3 不模拟订单执行、成本、滑点，也不产生业绩声明。

验证目标：

- 含明显未来 Bar 的夹具证明回放无法读取后续数据。
- 同一固定序列回放两次生成完全相同的有序观察结果。
- 移除截止点之后的 Bar 不会改变更早的回放决策。
- 回放输出会标记不可用/部分数据，不得静默跳过。

退出标准：历史回放可复现，并证明不存在未来数据泄漏。

## 6. 第 3 阶段：系统验证与验收

### M3.11 自动化测试矩阵

验证目标：

- 测试矩阵覆盖所有 M3 契约，包括失败与不可用路径，而不仅是指标公式的正常路径。
- 所需命令在干净 Gradle 调用中通过，且 M1/M2 回归编译保持通过。

| 范围 | 必需验证 |
| --- | --- |
| 领域数据质量 | 校验器问题/状态覆盖、元数据隔离、已确认 Bar 策略 |
| 指标 | 公式参考、预热、平坦/零输入、有限值、截止点不变性 |
| 形态 | 命中、临界未命中、重复状态、历史不足 |
| 规则 | 模板参数校验、条件组合、证据、幂等性 |
| 持久化 | DAO 查询、唯一信号键、Room 迁移、Repository 失败传播 |
| UI | 每种分析状态、模板规则流程、近期信号证据 |
| 回放 | 确定性、无前视、稳定的元数据/版本输出 |
| 回归 | M1/M2 行情、Provider、缓存、图表、单元测试和 Android 测试编译 |

每个实现出口必须执行：

```powershell
./gradlew.bat :app:testDebugUnitTest
./gradlew.bat :app:compileDebugAndroidTestKotlin
git diff --check
```

M3 收尾前还必须执行：

```powershell
./gradlew.bat :app:assembleDebug
```

退出标准：自动化验证证据附在 G6 候选发布记录中；任何跳过的测试必须记录风险和负责人。

### M3.12 人工验收场景

验证目标：

- 测试人员可在设备或模拟器复现核心日线 Bar 工作流，并区分有效信号与不可用分析。
- 人工证据确认用户可见的溯源、幂等、元数据隔离与回放一致性。

1. 打开一只拥有足够日线历史的代表性 A 股详情页，确认显示的分析截止点等于最后一个已确认日线 Bar。
2. 打开历史不足或历史无效的股票，确认不会把事件信号展示为有效。
3. 创建 EMA 交叉模板，对同一收盘 Bar 评估两次，确认只存在一条信号记录。
4. 切换日/周/月视图，确认结果不会混用周期、Provider 或复权元数据。
5. 检查已存信号，确认规则版本、源 Bar 时间、源数值和原因可见。
6. 对固定历史序列运行两次回放，确认信号日期和证据完全一致。

退出标准：所有场景通过；或在 M3 收尾前对每个偏差完成分诊并做出发布决策。

### M3.13 发布就绪清单

验证目标：

- 候选发布版从范围/契约决策到测试证据和设备验收均可追溯。
- 已知限制必须在产品行为中明确表达，不能用回退值或暗示性保证掩盖。

- 所有 G0-G6 证据均链接至 `Development Notes.md`。
- 所有必需自动化命令串行通过。
- 不存在未解决的一级数据质量、未来数据泄漏、重复信号或 Room 迁移问题。
- 在新模拟器或物理设备完成全部人工场景。
- M3 限制仍清晰可见：日线优先、无自动交易、无历史分钟回放、不得宣称可预测盈利。

退出标准：批准 G6，且 M3 变更记录说明已交付范围、测试证据和延期事项。

## 7. 风险与决策

| 风险 | 决策/缓解措施 |
| --- | --- |
| Provider 协议变化或 K 线畸形 | 在解析器和分析边界校验，并保留结构化拒绝原因 |
| 最新日线 Bar 可能未完成 | 标记为部分/未确认，并阻断事件规则触发 |
| 市场节假日/停牌看起来像缺口 | 在引入交易日历前使用保守缺口状态 |
| 指标公式正确但读取未来数据 | 强制截止点不变性与逐 Bar 回放测试 |
| 重复命中导致记录泛滥 | 在未来通知冷却前使用不可变幂等键 |
| 规则增长后难以维护 | 先使用强类型模板条件，再定义条件树契约，后续才做通用编辑器 |
| 意外破坏 M2 缓存语义 | 保留 Provider/周期/复权缓存隔离，并运行 M2 回归测试 |
| 范围膨胀到量化选股 | 横截面因子、IC/IR、投资组合回测独立为后续里程碑 |

## 8. 变更记录

### 2026-08-28 - 创建 M3 生命周期计划

- 使用完整生命周期门禁创建 M3 专用实施计划：评估、需求、架构、增量实现、验证、人工验收和发布就绪。
- 将 M3 定义为单股票、已确认日线 Bar 分析和本地信号记录阶段。
- 为每个 M3 子阶段增加验证目标和退出标准。
- 明确延期历史日内回放、通知、自动交易和横截面 Alpha 研究。

### 2026-08-30 - 批准 M3.0.2 需求基线

- 冻结首版日线范围和四组确定性规则模板：EMA 交叉、RSI 超卖回升、放量突破、阳包阴/阴包阳。
- 在 `Development Notes.md` 定义每个模板的默认值、最小历史、事件语义、方向、证据与幂等行为。
- 将多周期条件、背离、日内回放、通知、自动交易与排序延期至后续里程碑。

### 2026-08-30 - 批准 M3.1-M3.3 架构基线

- 批准纯领域历史序列与校验边界、显式截止点策略、强类型指标/形态/规则分层，以及后续规则/信号持久化设计。
- 确认 M3.4 从可单元测试的领域校验开始，且不改变 M2 图表数据与缓存行为。
- G2 证据和详细所有权决策已记录在 `Development Notes.md`。

## 9. 持续记录模板

### 2026-08-30 - M3.0.2 已完成：首版需求

- 状态：G1 通过。首版规则契约和验收语义已记录于 `Development Notes.md`。
- 边界：只有完整、已确认的日线 Bar 序列可用于事件规则；可见趋势状态本身不是信号。
- 后续要求：M3.1-M3.3 必须在功能实现前设计强类型历史序列、分析、持久化和 UI 契约。
- 验证结果：每个初始模板均已定义输入、最小历史、条件、方向、证据和幂等行为。

### 2026-08-30 - M3.1-M3.3 已完成：架构基线

- 状态：G2 通过。历史数据、质量、分析、持久化和 UI 的所有权边界已记录于 `Development Notes.md`。
- 设计决策：`HistoricalBarValidator` 是纯 Kotlin 并返回结构化数据；它不能写 Room、改变图表数据或决定 UI 展示。
- 后续要求：M3.4 实现校验器与测试夹具，之后由 Repository 创建元数据完整的输入且不改变 M2 图表渲染。
- 验证结果：架构保留 Provider/周期/复权隔离，并为每次后续计算提供显式截止点。

### 2026-08-30 - M3.4 已完成：校验边界与 Repository 适配

- 增加纯 Kotlin 历史序列契约、结构化质量结果、确定性质量优先级、JVM 夹具和 `StockDetailRepository.fetchHistoricalBarSeries()`。
- 保持 `fetchCandles()` 不变，作为 M2 图表/缓存路径；新 M3 路径附加 Provider 与市场元数据，从市场时段推导截止 Bar 完成状态，并在分析消费者获取前校验。
- 验证通过：`:app:testDebugUnitTest`（`68 tests completed`）和 `git diff --check`。
- 退出状态：G3 通过。无效数据保持为结构化结果，不能被当作可分析的完整序列。

### 2026-08-30 - M3.5 已完成：指标注册表与基础计算器

- 状态：G4 通过。增加强类型指标注册表及纯 Kotlin 的 SMA、EMA、成交量 SMA、MACD、RSI、布林带、ATR、OBV 计算器。
- 设计结果：每个输出均按时间戳对齐，并明确标记为 `WARMUP`、`VALUE` 或 `UNAVAILABLE`；缺失输入与无效输入原因不同，且没有计算器会读取输入截止点之后的数据。
- 验证通过：`:app:testDebugUnitTest` 和 `git diff --check`，包括手算夹具和跨指标截止点不变性。
- 后续要求：M3.6 实现强类型技术状态与形态检测器；更广泛的参考数据矩阵保留至 M3.11。

### 2026-08-30 - M3.6 已完成：技术状态与形态检测器

- 增加 EMA 趋势/交叉、RSI、成交量、布林带位置、OBV 方向、K 线几何、吞没与区间突破的强类型、携带证据的输出。
- 交叉事件与持续趋势状态分离，防止关系未改变时重复发出事件。
- 仅增加 Swing Point 契约；背离等待经过评审的算法与确定性夹具后再实现。
- 验证通过：`:app:testDebugUnitTest` 和 `git diff --check`，覆盖命中、临界未命中、历史不足和单次交叉。
- 后续要求：M3.7 使用这些领域输出评估强类型规则模板并创建幂等信号身份。

### 2026-08-30 - M3.7 已完成：规则评估与信号幂等

- 状态：G5 通过。增加强类型规则树、确定性评估状态、证据传播、首版模板和版本化信号键。
- 设计结果：被质量阻断的数据永远不能产生最终命中事件；同一规则上下文的重复评估保持幂等，不引入通知冷却行为。
- 验证通过：`:app:testDebugUnitTest`（`87 tests completed`）和 `git diff --check`。
- 后续要求：M3.8 经由 Room 持久化规则/信号并暴露用例，同时保持领域评估器为唯一事实来源。

### 2026-08-30 - M3.8 已完成：Room Repository 与评估用例

- 增加 Room v7 规则/信号实体、DAO、显式 `6 -> 7` 迁移、领域映射器、Repository 和 `EvaluateEnabledRules`。
- 设计结果：规则 JSON 保留强类型条件树，证据 JSON 保留审计细节，信号复合主键在存储层强制幂等。
- 验证通过：`:app:testDebugUnitTest`、`:app:compileDebugAndroidTestKotlin` 和 `git diff --check`。
- 新 DAO 测试仍需在 M3.11/M3.12 的设备上执行；本次未启动模拟器。
- 后续要求：M3.9 在这些用例之上增加只读详情摘要与模板规则 UI。

### 2026-08-30 - M3.9 已完成：详情分析摘要与模板 UI

- 在详情 ViewModel 增加分析状态和独立的日线历史请求，独立于 Time/日/周/月图表选择，避免默认 Time 图表错误成为规则分析输入。
- 增加只读质量/溯源/截止点/证据展示和六个模板启用操作。无效或部分历史可见，但绝不会被展示为有效分析事实。
- 验证通过：`:app:testDebugUnitTest`、`:app:compileDebugAndroidTestKotlin` 和 `git diff --check`。
- Compose 与 Room 的设备执行仍是 M3.12 的要求；本次只编译了 Android 测试。
- 后续要求：M3.10 基于已校验日线 Bar 实现确定性历史回放。

### 2026-08-30 - M3.10 已完成：历史规则回放

- 增加逐 Bar 回放和 `ReplayRuleHistory` 用例边界。每根源 Bar 都会得到已评估、历史不足、部分或被阻断结果，而不是被静默跳过。
- 已验证重复回放的确定性，以及追加未来 Bar 时不存在前视影响。
- 验证通过：`:app:testDebugUnitTest` 和 `git diff --check`。
- 后续要求：M3.11 执行完整自动化测试矩阵和 M1/M2 回归编译。

### 2026-08-30 - M3.11 已完成：自动化验证矩阵

- 已交付：在 M3.10 实现后重新运行 M3 JVM 单元测试、Android 测试 Kotlin 编译、Debug APK 组装和空白字符检查。
- 验证：`gradlew.bat :app:testDebugUnitTest :app:compileDebugAndroidTestKotlin :app:assembleDebug` -> 通过；`git diff --check` -> 通过。
- 执行说明：Gradle 复用了配置缓存，所有请求任务均为 `UP-TO-DATE`；这确认当前输出，但不等同于干净构建结果。
- 已知限制/下一门禁：Room DAO 测试和 Compose 分析 UI 测试仅完成编译，仍需在替换后的模拟器或物理设备上执行 M3.12。

### 2026-08-28 - M3.0.1 已完成：现状评估

- 状态：G0 通过。评估记录和源到分析边界证据已记录于 `Development Notes.md`。
- 复用决策：保留现有归一化 `Candle`、Provider 选择、Provider/周期/复权缓存隔离和 Room K 线缓存。
- 后续要求：M3.1 在实现计算前引入显式历史序列溯源、数据质量结果和最新日线 Bar 确认。
- 验证结果：M3.0.1 所有验证目标均已满足；历史分钟回放和不受支持的 Provider 能力保持在范围之外。

每完成一个 M3 阶段后追加一条记录：

```markdown
### YYYY-MM-DD - M3.x 已完成：<阶段名称>

- 已交付范围：<改动内容>。
- 契约/决策：<重要决策及其理由>。
- 验证：`<命令或场景>` -> <结果>。
- 已知限制/下一门禁：<剩余条件>。
```
